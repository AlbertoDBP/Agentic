# Server
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# MySQL (Hostinger)
DB_HOST=localhost
DB_PORT=3306
DB_USER=charity_user
DB_PASSWORD=change_me
DB_NAME=charity_inventory

# Auth
JWT_SECRET=change_me_to_a_long_random_secret
JWT_EXPIRES_IN=24h

# CORS (comma-separated origins, or * for dev)
CORS_ORIGIN=*
